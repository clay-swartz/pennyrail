# PennyRail Radar v17

Full-project build based on v16.

Changes:
- Radar parser now accepts current/top-level `radar` arrays plus clusters/wishes/items/results fallbacks.
- Adds `feedShape` diagnostics so future Agent402 schema changes are visible immediately.
- Keeps v16 scan timeouts, visible browser errors, Base mainnet x402 config, and full project structure.
